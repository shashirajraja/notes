#include<stdio.h>
#include<conio.h>

int main() {
	
    if(1){} //always true
	
	if(0) {} //always false
	
	if(/*any number or statement whose value is not 0*/) {
		
		//always true
		//always execute
		
	} else {
		//never execute
	}
	
	
	if(0) {
		//never true
	}
	else {
		//always execute
	}
	
	
	//else {
		//else without a previous if is error	
	//}
	
}


