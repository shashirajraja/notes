#include<stdio.h>
#include<conio.h>
int main(){
	char ch = 'p';
	
	int x = ch; //it will store the ascii value of character ch into x // x = 97
	
	char y = x; // it will again store the character of that ascii value  // y = a
	
	
	printf("\nValue of x = %d",x);
	printf("\nValue of y = %c",y);
	
	return 0;
}
